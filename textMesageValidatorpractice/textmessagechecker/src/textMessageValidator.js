import logo from './logo.svg';
import React, { useState } from 'react';
import './css/styles.css';

export default function  textMessageValidator() {
  /*
  const [message, setMessage] = React.useState('Invalid message');
  function textMessageValidator(event) {
    const value = event.target.value
    if ((value.trim().length) < 3) {
      setMessage("Invalid message")

    }
    else {
      setMessage("Valid message")
    }
  }

  function counter(event){
    if(event){
      alert("clicked")
    }
    else if (!event){
      console.log("not clicked")
    }
  }
  return (
    <><form>
      <label>Your message</label>
      <input type="text" onChange={textMessageValidator} />
      <p id="label">{message}</p>
    </form>
    <button onClick={counter}>
      Hello
      </button></>
  );
  */
}